# Покрокова інструкція: Деплой Sieve на Vercel

## Крок 1: Скопіюй файли в проєкт (5 хвилин)

1. **Відкрий Finder і знайди папку проєкту:**
   `/Users/mariazdebska/Desktop/Sieve`

2. **Скопіюй три файли з Downloads в папку Sieve:**
   - `.gitignore` → в корінь `/Sieve/`
   - `.env.example` → в корінь `/Sieve/`
   - `README.md` → в корінь `/Sieve/`

**Важливо:** Файл `.gitignore` починається з крапки — щоб його побачити в Finder, натисни `Cmd + Shift + .` (покаже приховані файли)

---

## Крок 2: Перевір .env.local (2 хвилини)

1. Відкрий файл `/Sieve/.env.local` у текстовому редакторі
2. Переконайся, що там є всі змінні:

```env
NEXT_PUBLIC_SUPABASE_URL=https://bosjoczzpjmmirotuwfz.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=твій_ключ
ANTHROPIC_API_KEY=твій_ключ
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=твій_секрет
```

3. **Якщо немає `NEXTAUTH_SECRET`** — згенеруй його:
   - Відкрий Terminal
   - Виконай: `openssl rand -base64 32`
   - Скопіюй результат
   - Додай у `.env.local`: `NEXTAUTH_SECRET=скопійований_результат`

---

## Крок 3: Ініціалізуй Git (3 хвилини)

Відкрий Terminal і перейди в папку проєкту:

```bash
cd /Users/mariazdebska/Desktop/Sieve
```

Виконай команди по черзі:

```bash
# Ініціалізувати Git (якщо ще не зробила)
git init

# Додати всі файли (крім тих, що в .gitignore)
git add .

# Перевірити, що .env.local НЕ доданий
git status

# Якщо бачиш .env.local — видали його з Git:
git rm --cached .env.local

# Зробити перший commit
git commit -m "Initial commit: Vamos Hiring System (Sieve)"
```

**Перевір:** Виконай `git status` — має бути написано "nothing to commit, working tree clean"

---

## Крок 4: Створи GitHub репозиторій (3 хвилини)

1. Йди на https://github.com/new
2. Заповни:
   - **Repository name:** `vamos-hiring-system` (або `sieve`)
   - **Visibility:** Private ✅ (щоб ніхто не бачив код)
   - **НЕ** ставити галочки "Add README", "Add .gitignore" — у тебе вже є
3. Натисни **"Create repository"**

4. GitHub покаже команди — скопіюй їх і виконай у Terminal:

```bash
git remote add origin https://github.com/твій-username/vamos-hiring-system.git
git branch -M main
git push -u origin main
```

**Введи свій GitHub username і password/token коли попросить**

**Перевір:** Оновити сторінку на GitHub — має з'явитись код проєкту

---

## Крок 5: Деплой на Vercel (5 хвилин)

### 5.1 Зареєструйся на Vercel

1. Йди на https://vercel.com/signup
2. Натисни **"Continue with GitHub"**
3. Дозволь Vercel доступ до твого GitHub

### 5.2 Імпортуй проєкт

1. На головній Vercel → натисни **"Add New"** → **"Project"**
2. Знайди репозиторій `vamos-hiring-system` у списку
3. Натисни **"Import"**

### 5.3 Налаштуй Environment Variables

**ВАЖЛИВО!** Vercel попросить додати змінні оточення. Скопіюй з твого `.env.local`:

Натисни **"Environment Variables"** і додай по одній:

```
Name: NEXT_PUBLIC_SUPABASE_URL
Value: https://bosjoczzpjmmirotuwfz.supabase.co
```

```
Name: NEXT_PUBLIC_SUPABASE_ANON_KEY
Value: [твій ключ з .env.local]
```

```
Name: ANTHROPIC_API_KEY
Value: [твій ключ з .env.local]
```

```
Name: NEXTAUTH_SECRET
Value: [твій секрет з .env.local]
```

```
Name: NEXTAUTH_URL
Value: https://твій-проєкт.vercel.app
```

**Примітка:** `NEXTAUTH_URL` поки що залиш тимчасово як `https://vamos-hiring-system.vercel.app` — після деплою оновимо на реальний URL.

**Для кожної змінної:** вибери "Production, Preview, and Development" (всі три)

### 5.4 Запусти деплой

1. Натисни **"Deploy"**
2. Зачекай 1-3 хвилини (Vercel компілює проєкт)
3. Побачиш "Congratulations!" 🎉

---

## Крок 6: Оновлення після деплою (2 хвилини)

1. **Скопіюй свій Vercel URL** — наприклад, `vamos-hiring-system.vercel.app`

2. **Оновити `NEXTAUTH_URL`:**
   - У Vercel → Settings → Environment Variables
   - Знайди `NEXTAUTH_URL`
   - Натисни "Edit"
   - Зміни на `https://vamos-hiring-system.vercel.app` (твій реальний URL)
   - Натисни "Save"
   - Vercel запропонує "Redeploy" — натисни "Redeploy"

3. **Зачекай ~1 хвилину** (повторний деплой)

---

## Крок 7: Перевір, що все працює (3 хвилини)

Відкрий свій Vercel URL у браузері:

### 7.1 Перевір публічну частину:
- `https://твій-url.vercel.app/` — лендінг має завантажитись
- `https://твій-url.vercel.app/apply` — форма заявки
- Заповни форму тестовими даними → має зберегтися

### 7.2 Перевір dashboard:
- `https://твій-url.vercel.app/dashboard` — має попросити логін
- Введи: `manager@vamos.com` / `test123`
- Має відкритись dashboard з твоїми даними

### 7.3 Якщо щось не працює:
- Vercel → Deployments → твій деплой → "View Function Logs"
- Скопіюй помилку і надішли мені — розберемось

---

## Крок 8: Workflow для подальшої роботи

### Тепер кожна зміна — це просто:

```bash
# 1. Відредагувала файли в Claude Code

# 2. Коли готова до тесту:
cd /Users/mariazdebska/Desktop/Sieve
git add .
git commit -m "опис того, що зробила"
git push

# 3. Через 30-60 секунд — зміни на Vercel
# 4. Оновити сторінку на своєму URL — побачиш зміни
```

**Твій MacBook тепер тільки для редагування — вся компіляція на Vercel!** 🚀

---

## Troubleshooting

### Якщо Git не пушить:
```bash
# Якщо попросить логін:
git config --global user.email "твій@email.com"
git config --global user.name "Твоє Ім'я"
```

### Якщо Vercel показує помилку про змінні:
- Settings → Environment Variables
- Переконайся, що всі 5 змінних додані
- Натисни "Redeploy"

### Якщо NextAuth не логінить:
- Перевір, що `NEXTAUTH_URL` правильний (з https://)
- Перевір, що `NEXTAUTH_SECRET` не пустий
- Redeploy після зміни

---

## ✅ Checklist

- [ ] Скопіювала 3 файли в проєкт
- [ ] Згенерувала `NEXTAUTH_SECRET`
- [ ] Зробила `git init` + `git add` + `git commit`
- [ ] Створила GitHub репозиторій
- [ ] Запушила код на GitHub
- [ ] Зареєструвалась на Vercel
- [ ] Імпортувала проєкт
- [ ] Додала всі 5 environment variables
- [ ] Зробила перший деплой
- [ ] Оновила `NEXTAUTH_URL`
- [ ] Перевірила, що сайт працює

---

**Коли зробиш всі кроки — надішли мені посилання на Vercel URL!** 🎉

Якщо щось не виходить на будь-якому кроці — пиши, допоможу!
